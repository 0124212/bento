# Vibe workspace

This is a sandboxed coding workspace. Follow these rules strictly:

1. WRITE ONLY inside this workspace folder:
   C:\Users\asher\Documents\CODE\bento
   Everything you create, edit, or delete must live under this folder.
2. You MAY read anything on this computer and MAY use any installed program,
   tools, or libraries you need (git, compilers, runtimes, package managers).
3. Never create, modify, or delete files or folders outside this workspace —
   including the user's home directory, other projects, or system files.
   If a task needs to change something outside this workspace, ask the user.
4. Keep all git repos, scripts, and artifacts inside this workspace.
5. Assume a VPN is active; never disable it or modify networking.
